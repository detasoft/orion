interface:
  display_name: "Orion Minimal Implementation"
  short_description: "Implement and review with fewer concepts"
  default_prompt: "Use $orion-minimal-implementation to implement this change in small verified parts, then summarize the problem, solution, affected parts, and verification."
