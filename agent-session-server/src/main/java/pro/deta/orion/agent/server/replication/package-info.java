/**
 * Transport-neutral durable session journal replication. Cursors and
 * acknowledgements are derived from the journal's persistence boundary.
 */
package pro.deta.orion.agent.server.replication;
