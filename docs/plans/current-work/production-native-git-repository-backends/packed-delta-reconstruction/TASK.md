# Reconstruct Packed Delta Objects

Status: done
Source: converted from former production repository backends child task.

Reconstruct packed `OFS_DELTA` and `REF_DELTA` objects on demand.

- [x] Reconstruct packed `OFS_DELTA` and `REF_DELTA` objects on demand.
